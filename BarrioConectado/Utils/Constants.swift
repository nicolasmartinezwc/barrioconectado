//
//  Constants.swift
//  BarrioConectado
//
//  Created by Nicolas Martinez on 24/09/2024.
//

import SwiftUI

enum Constants {
    enum Tabs {
        static let home: Int = 1
        static let events: Int = 2
        static let alerts: Int = 3
        static let exchanges: Int = 4
        static let configuration: Int = 5
    }

    enum Colors {
        static let appColor: Color = .init(hex: "#2ead80")
        static let backgroundGrayColor: Color = .init(hex: "#f5f5f5")
        static let backgroundDarkGrayColor: Color = .init(hex: "#f2f2f2")

        // Peach
        static let peach: Color = .init(hex: "#fae0cf")
        static let darkPeach: Color = .init(hex: "#fcc781")

        // Caramel
        static let caramel: Color = .init(hex: "#efa888")

        // Nature
        static let nature: Color = .init(hex: "#eafbf1")
        static let darkNature: Color = .init(hex: "#2ca57c")

        // Yellow
        static let yellow: Color = .init(hex: "#f8fa93")

    }
}
